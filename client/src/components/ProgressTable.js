import React, { useState } from 'react';
import { Modal, Button, Form } from 'react-bootstrap';
import axios from 'axios';

const ProgressTable = ({onSignInSuccess}) => {

  return (
    <div>
      <h1>Schedule</h1>
        This is a tables
    </div>
  );
}

export default ProgressTable;